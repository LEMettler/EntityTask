<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comhummeltronentity_task.httpsgithub.entitytask">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activity_classes.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_classes.CalendarActivity" />
        <activity
            android:name=".activity_classes.TasksActivity"
            android:label="@string/title_activity_tasks"
            android:theme="@style/AppTheme.NoActionBar"
        />

        <activity android:name=".activity_classes.TaskcreatorActivity"></activity>
    </application>

</manifest>